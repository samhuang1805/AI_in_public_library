streamlit>=1.9.2
folium==0.12.1  # Updated to a known stable version
streamlit-folium==0.4.0  # Updated to match the folium version
pandas==1.3.3  # Downgraded to a stable version compatible with numpy
openpyxl==3.0.10
gspread==5.0.0  # Slightly older version for compatibility
oauth2client==4.1.3
numpy==1.21.0  # A more compatible version with pandas
